{
    "uuid": "todo_desklet@neoz666",
    "name": "To-Do List Desklet",
    "description": "A desklet to manage your tasks with priorities.",
    "settings": [
        {
            "key": "default_priority",
            "type": "enum",
            "name": "Default Task Priority",
            "description": "Set the default priority for new tasks.",
            "options": [
                ["High", "High"],
                ["Medium", "Medium"],
                ["Low", "Low"]
            ],
            "default": "Medium"
        },
        {
            "key": "show_completed_tasks",
            "type": "boolean",
            "name": "Show Completed Tasks",
            "description": "Toggle the visibility of completed tasks.",
            "default": true
        },
        {
            "key": "font_size",
            "type": "int",
            "name": "Font Size",
            "description": "Set the font size for task labels.",
            "default": 14,
            "range": [10, 20]
        }
    ]
}

